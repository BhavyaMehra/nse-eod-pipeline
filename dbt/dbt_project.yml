name: 'nse_eod_pipeline'
version: '1.0.0'
config-version: 2

profile: 'nse_eod_pipeline'

model-paths: ["models"]
log-path: "logs"
target-path: "target"
clean-targets: ["target"]

models:
  nse_eod_pipeline:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts